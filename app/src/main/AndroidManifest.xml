<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mtking">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_api_key" />

        <activity
            android:name=".activity.CuaHangActivity"
            android:label="Chỗ cửa hàng " />
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DangKyActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.DangNhapActivity" />
        <activity android:name=".activity.GioHangActivity" />
        <activity android:name=".activity.ChungToiActivity"></activity>
        <activity android:name=".activity.HeThongActivity" />
        <activity android:name=".activity.GmailActivity" />
        <activity android:name=".activity.QuanLyMKActivity" />
        <activity android:name=".activity.SDTActivity" />
        <activity android:name=".activity.QuanLyDiaChiActivity" />
        <activity android:name=".activity.DiaChiGiaoHangActivity" />
        <activity android:name=".activity.ThanhVienActivity" />
        <activity android:name=".activity.QuaTangActivity" />
        <activity android:name=".activity.DoiDiemActivity" />
        <activity android:name=".activity.CDTLuyActivity" />
        <activity android:name=".activity.HistoryActivity" />
        <activity android:name=".activity.SettingActivity" />
        <activity android:name=".activity.NX_PhanHoiActivity" />
        <activity android:name=".activity.product_detailsActivity" />
        <activity android:name=".activity.DrinkActivity" />
    </application>

</manifest>